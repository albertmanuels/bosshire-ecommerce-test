/**
 * Number of milliseconds in one minute.
 *
 * Useful for setting time-based durations such as timeouts,
 * cache expiry, intervals, or cookie/session lifetimes.
 *
 * @constant
 * @example
 * setTimeout(() => console.log("1 minute passed"), ONE_MINUTE);
 */
export const ONE_MINUTE = 60 * 1000